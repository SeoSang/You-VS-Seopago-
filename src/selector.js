// redux-saga 에서 state 접근을 위한 함수

export const map = (state) => state.map
export const turn = (state) => state.turn
export const score = (state) => state.score
export const candidate = (state) => state.candidate
